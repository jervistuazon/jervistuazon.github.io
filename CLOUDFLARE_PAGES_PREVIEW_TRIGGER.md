# Cloudflare Pages staging preview trigger

This file is intentionally non-runtime documentation. It exists to trigger the Git-integrated Cloudflare Pages preview deployment for `codex/cloudflare-pages-staging` without changing the tested portfolio behavior.
